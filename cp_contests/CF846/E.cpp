#include<bits/stdc++.h>
using namespace std;
using ll = long long;
/*
    number * x >= l && number * (x + 1) <= r
    number * x >= l && number * x + number <= r 
    So lets assume first one is l1
    l1 == l

    l1 + number <= r 

    &&
    l1 < l + number;

    Therefore

*/
void solve(){
    ll l, r;
    cin >> l >> r;
    ll x
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    int t;
    cin >> t;
    while(t--) solve();
}