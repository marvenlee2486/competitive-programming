#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    // string s;
    // while(cin >> s && s != "0"){
    //     s = s.substr(0, s.size() - 3);
    //     ld x = stold(s);
    //     ld y = x * 10;
    //     x /= 10;

        
    // }
    ld x = 1186531 * 1.0 / 2500000;
    cout << fixed << setprecision(15) << x << "\n";
}